<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <record id="view_account_payment_form" model="ir.ui.view">
        <field name="name">account.payment.form.inherited</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_form"/>
        <!-- Se comenta el campo que hace referencia a transferencias internas entre cuenta ya que en odoo 18 no existe el campo destination_journal_id -->
        <!--field name="arch" type="xml">
            <field name="destination_journal_id" position="after">
                <div colspan="2" invisible="payment_method_code != 'withholding'">
                    <group name="withholding_data">
                        <field name="tax_withholding_id" options="{'no_create': True, 'no_open': True}" domain="[('type_tax_use', '=', partner_type), ('company_id', '=', company_id)]" required="payment_method_code == 'withholding'"/>
                        <field name="withholding_number"/>
                        <field name="withholding_base_amount" required="payment_method_code == 'withholding'"/>
                    </group>
                </div>
            </field>
        </field-->
    </record>
</odoo>
